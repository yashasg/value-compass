"""vca-api — the FastAPI application package.

See ``backend/api/README.md`` for the full spec; the actual app object is
defined in :mod:`api.main` and is referenced by the systemd unit as
``api.main:app``.
"""
