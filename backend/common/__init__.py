"""Shared configuration, constants, and utilities for value-compass backend services.

This package is imported by both ``backend/api`` and ``backend/poller`` —
they must never drift, so anything reused between them lives here.
"""
